#include <bits/stdc++.h>

using namespace std;

inline void testCase();

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    int t;
    cin >> t;
    while (t-- > 0) {
        testCase();
    }
}

inline void testCase() {
    int n;
    cin >> n;
    for (int i = 1; i <= n; i++) {
        cout << "1 ";
    }
    cout << endl;
}
