#include <bits/stdc++.h>

using namespace std;

typedef unsigned long long bigint;

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    bigint A, B, C, D;
    cin >> A >> B >> C >> D;
    cout << A << " " << B << " " << C << " " << D << endl;
}
